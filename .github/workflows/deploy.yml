name: Deployment
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build_and_push_image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build and push nodejs app image
        uses: ./.github/actions/BuildPushImage
        with:
          image-name: sysops-nodejs-app
